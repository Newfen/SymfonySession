{% extends 'base.html.twig' %}

{% block title %}
Session
{% endblock %}

{% block body %}

    {% if module.programmes | length > 0 %}
    {% for programme in module.programmes %}
    {{ programme.LibelleCategorie }}

    {% endfor %}
    {% else %}
    Aucune Module
    {% endif %}

{% endblock %}

